<html>
<head>
<%- include('common/head') %>
</head>
<body>
    <div class="main">
        <h1>Files</h1>

        <ul>
            <% files.forEach(function(file) { %>
            <li><a href="<%= file %>"><%= file %></a></li>
            <% }); %>
        </ul>
    </div>
</body>
</html>
